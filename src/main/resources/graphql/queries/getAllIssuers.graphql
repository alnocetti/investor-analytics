query getAllIssuers($input: GetIssuersInput) {
    getIssuers(input: $input) {
        content {
            id
            name
        }
        paginationInfo {
            totalElements,
            totalPages,
            page,
            size
        }
    }
}